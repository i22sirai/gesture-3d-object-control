# 手のジェスチャーによる3Dオブジェクト操作インターフェース

## 概要

Webカメラで取得した手の動きを用いて、Unity上の3Dオブジェクトを操作するインターフェースを開発しています。

MediaPipe Handsを用いて手のランドマークを取得し、Python側で生成した操作情報をUDP通信によってUnityへ送信します。Unity側では、受信した情報を3Dオブジェクトの拡大縮小と回転に反映します。

本研究では、キーボードやマウスを使わず、手の動きによって3Dオブジェクトを直感的に操作できるインターフェースの実現を目指しています。

本プロジェクトは、卒業研究として開発中のプロトタイプです。

---

## 研究の背景・目的

一般的な3D編集ソフトでは、キーボードやマウスを用いて、操作対象や機能を選択しながら編集を行います。そのため、初めて使用する人にとっては、操作方法を覚えるまでに時間がかかる場合があります。

本研究では、手のジェスチャーを入力として用いることで、初めて使用する人でも操作内容を理解しやすく、学習負担や誤操作の少ない3D操作インターフェースを目指しています。

特に、ユーザーの手の動きや操作意図を、どのように3D空間上の変化へ対応させるかを重視しています。

---

## 使用技術

- Unity
- C#
- Python
- MediaPipe Hands
- OpenCV
- UDP通信
- Webカメラ

---

## システム構成

```text
Webカメラ
    ↓
Python / OpenCV / MediaPipe Hands
    ↓
手のランドマーク取得
    ↓
ジェスチャーおよび操作情報の生成
    ↓ UDP通信
Unity / C#
    ↓
3Dオブジェクトへの操作反映
```

Python側では、Webカメラの映像からMediaPipe Handsを用いて手のランドマークを取得します。

取得した両手の位置関係や距離の変化から操作情報を生成し、UDP通信によってUnityへ送信します。

Unity側では、Pythonから受信した情報をもとに、3Dオブジェクトの拡大縮小や回転をリアルタイムに反映します。

---

## ファイル構成

```text
gesture-3d-object-control/
├── main.py
├── hand_gesture.py
├── UDPReceiver.cs
├── README.md
├── LICENSE
└── .gitignore
```

### `main.py`

次の処理を行います。

- Webカメラ映像の取得
- MediaPipe Handsによる手の認識
- 手のランドマーク情報の取得
- 操作情報の生成
- UDP通信によるUnityへのデータ送信

### `hand_gesture.py`

手のランドマークを用いたジェスチャー判定に関する処理を行います。

### `UDPReceiver.cs`

Pythonから送信された操作情報をUnity側で受信し、3Dオブジェクトの拡大縮小や回転に反映します。

---

## 現在の実装状況

現時点では、次の機能を実装しています。

- MediaPipe Handsによる両手のランドマーク取得
- PythonからUnityへのUDP通信
- 両手の距離変化を利用した3Dオブジェクトの拡大縮小
- 手の動きを利用した3Dオブジェクトの回転
- Unity上の3Dオブジェクトへのリアルタイムな操作反映

現在は、拡大縮小と回転操作の安定性を改善しています。

選択、移動、3層構造については、現時点では未実装です。

---

## 実行環境

現在の開発環境は以下のとおりです。

- Python 3.x
- Unity
- Webカメラ

使用しているバージョンは、確認後に以下へ追記します。

```text
Python:
Unity:
OS:
```

---

## 実行方法

### 1. リポジトリを取得する

Gitを使用する場合は、次のコマンドを実行します。

```bash
git clone https://github.com/i22sirai/gesture-3d-object-control.git
```

取得したフォルダへ移動します。

```bash
cd gesture-3d-object-control
```

Gitを使用しない場合は、GitHub上の「Code」からZIPファイルをダウンロードし、展開してください。

---

### 2. Pythonライブラリをインストールする

必要なライブラリをインストールします。

```bash
pip install opencv-python mediapipe
```

使用環境によっては、追加のライブラリが必要になる場合があります。

今後、必要なライブラリを整理した `requirements.txt` を追加する予定です。

---

### 3. Unity側を準備する

1. Unityで3Dプロジェクトを作成または開きます。
2. 操作対象となる3Dオブジェクトをシーン内に配置します。
3. `UDPReceiver.cs` をUnityプロジェクトの `Assets` フォルダへ追加します。
4. `UDPReceiver.cs` を操作対象のGameObjectにアタッチします。
5. Unityのシーンを実行します。

現在、このリポジトリにはUnityプロジェクト全体ではなく、主要なC#スクリプトを掲載しています。

---

### 4. Python側を実行する

Unityのシーンを実行した状態で、ターミナルから次のコマンドを実行します。

```bash
python main.py
```

Webカメラが起動し、MediaPipe Handsによる手の認識が開始されます。

取得した操作情報はUDP通信によってUnityへ送信され、3Dオブジェクトの拡大縮小や回転に反映されます。

---

### 5. カメラが起動しない場合

使用する環境によって、Webカメラの番号が異なる場合があります。

`main.py` 内の次の部分を確認してください。

```python
cv2.VideoCapture(1)
```

内蔵カメラを使用する場合は、次のように `0` へ変更すると起動することがあります。

```python
cv2.VideoCapture(0)
```

使用するカメラに応じて番号を変更してください。

---

## 操作方法

### 拡大縮小

両手の距離の変化を利用して、3Dオブジェクトの大きさを変更します。

- 両手の距離を広げる：拡大
- 両手の距離を近づける：縮小

### 回転

手の動きや位置関係を利用して、3Dオブジェクトを回転させます。

現在は、操作時のブレや意図しない回転を減らすため、判定方法や回転量の調整を進めています。

---

## デモ動画

現在準備中です。

今後、次の内容が分かる動画を掲載する予定です。

- MediaPipe Handsによる両手の認識
- 手の動きに応じた拡大縮小
- 手の動きに応じた回転
- PythonとUnity間のリアルタイムな連携

動画を追加した後は、以下の部分へURLを掲載します。

```markdown
[デモ動画を見る](ここに動画URLを入力)
```

---

## 今後の実装予定

今後は、次の機能を実装する予定です。

- 拡大縮小操作の安定化
- 回転操作の安定化
- オブジェクトの選択
- オブジェクトの移動
- 誤操作を抑えるための判定処理
- 操作状態を分かりやすくする表示
- 3層構造による操作体系の整理
- 既存の操作方法との比較評価

---

## 3層構造について

今後は、複数の3D編集操作を段階的に扱うため、操作体系に3層構造を導入する予定です。

3層構造を用いることで、限られたジェスチャーを複数の操作に対応させながら、ユーザーが現在どの段階の操作を行っているのかを理解しやすくすることを目指します。

具体的な各層の役割や操作方法については、現在設計および実装を進めています。

実装内容が確定した後、本項目に各層の詳細を追記します。

---

## 今後の評価

システム完成後は、既存の操作方法との比較評価を行う予定です。

主に次の項目を評価します。

- 操作完了までにかかる時間
- 誤操作の回数
- 操作方法を理解するまでの学習負担
- 操作の分かりやすさ
- 使用時の疲労感
- 主観的な使いやすさ

これらの結果から、ジェスチャー操作の有効性と改善点を検討します。

---

## 工夫している点

- 両手の距離変化を拡大縮小操作に対応させている点
- 手の動きと3Dオブジェクトの変化をリアルタイムに連携させている点
- PythonとUnityをUDP通信で接続している点
- 単に動作させるだけでなく、操作の分かりやすさや誤操作の少なさを重視している点
- 今後の操作追加を想定し、3層構造による操作体系を検討している点

---

## 現在の課題

現在、次の課題があります。

- 手の認識結果による値のブレ
- 拡大縮小時の変化量の調整
- 回転方向および回転量の安定化
- 意図しない操作の発生
- 複数の操作を使い分ける方法の整理
- 初めて使用する人にも分かりやすい操作体系の設計

今後は、判定条件の調整や値の平滑化などを行い、操作の安定性を改善します。

---

## 開発における生成AIの活用

本研究では、Unity、MediaPipe Hands、UDP通信について学習しながら開発を進めています。

研究期間に限りがあり、周囲に関連技術について詳しく相談できる人が少ないため、次の用途で生成AIを補助的に活用しています。

- 開発環境構築の手順確認
- エラー原因の調査
- 実装方法の検討
- コードの処理内容に関する疑問点の確認
- 改善方法の検討

生成された内容をそのまま使用するのではなく、動作を確認し、研究目的に合わせて修正・統合しながら開発しています。

---

## 開発状況

本プロジェクトは現在も開発中です。

現時点では、拡大縮小と回転の基本操作を実装しています。今後は、操作の安定化、選択・移動操作、3層構造、評価実験を進めます。

---

## ライセンス

このリポジトリはMIT Licenseのもとで公開しています。

詳細は [LICENSE](LICENSE) を確認してください。

## 開発状況

本プロジェクトは卒業研究として開発中のプロトタイプです。
